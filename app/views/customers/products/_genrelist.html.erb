<div class="col-xs-2">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>ジャンル検索</th>
      </tr>
    </thead>
    <tbody>
      <% genres.each do |genre| %>
        <tr>
          <td><%= link_to genre.name, #リンク %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
